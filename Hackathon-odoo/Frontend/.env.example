# Frontend environment variables (do NOT commit real values)
# VITE_ prefix is required for Vite to expose them to the client
VITE_API_URL=https://your-backend.onrender.com/api
# Add any additional VITE_ variables your app needs, e.g.:
# VITE_OTHER_VAR=your-value

